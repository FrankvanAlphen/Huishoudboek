# --- Verplicht voor gedeelde, blijvende opslag ---
# Plak hier de interne Database URL van je Railway Postgres-service.
# (Op Railway kun je ook de waarde ${{Postgres.DATABASE_URL}} gebruiken.)
DATABASE_URL=

# --- Het wachtwoord waarmee jij en Kimberley inloggen (kies zelf iets) ---
APP_PASSWORD=Huishouden2026

# --- Optioneel ---
# Alleen instellen als je database SSL vereist (Railway intern niet):
# DATABASE_SSL=true
# Eigen sleutel voor het inlog-cookie (anders automatisch afgeleid):
# APP_SECRET=
